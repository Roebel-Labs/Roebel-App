node_modules
.env
.env.*
!.env.example
coverage
coverage.json
typechain
typechain-types

#Hardhat files
cache*
artifacts*

# zKey artifacts (~1.5GB tarball + extracted zkeys, fetched by scripts/download-zkeys.sh)
zkeys/
*.tar.gz
*.zkey

# Deployment outputs — JSON address records are committed; everything else local
deployments/*
!deployments/*.json

# maci-contracts deploy-task scratch files (auto-created on hardhat boot, unused)
deploy-config.json
deployed-contracts.json
