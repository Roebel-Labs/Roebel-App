# Link workspace packages even when referenced by a semver range (not just the
# `workspace:` protocol). Lets the external mini-app scaffold depend on
# "@netizen/miniapp-sdk": "^0.1.0" (correct for standalone / external deploys)
# while still resolving to the local packages/miniapp-sdk during monorepo dev.
link-workspace-packages=true
