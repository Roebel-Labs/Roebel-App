/**
 * Event poster geometry shared by the rail cards, their skeletons and the
 * detail page. Most event images are A4 flyers, so the boxes are A4
 * portrait and `cover` shows them edge to edge without a crop.
 */

/** Rail poster width. */
export const POSTER_CARD_WIDTH = 256;
/** A4 portrait (210 × 297): width / height. */
export const POSTER_ASPECT_RATIO = 210 / 297;
export const POSTER_RADIUS = 10;
